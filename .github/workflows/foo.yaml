on:
  workflow_dispatch:

jobs:
  foo:
    runs-on: [self-hosted, noble, ARM64]
    steps:
      - run: sudo apt-get install cpu-checker
      - run: sudo apt-get install qemu-kvm libvirt-daemon-system libvirt-clients bridge-utils
      - run: sudo kvm-ok
