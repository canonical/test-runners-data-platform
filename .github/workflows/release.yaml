# Copyright 2023 Canonical Ltd.
# See LICENSE file for licensing details.
name: Release PG-K8S to Charmhub (Lucas Test)

on:
  workflow_dispatch:

jobs:
  build:
    name: Build charm
    uses: canonical/data-platform-workflows/.github/workflows/build_charm.yaml@lucas/test-cc3-edge

  release:
    name: Release charm
    needs:
      - build
    uses: canonical/data-platform-workflows/.github/workflows/release_charm.yaml@lucas/test-cc3-edge
    with:
      channel: latest/stable
      artifact-prefix: ${{ needs.build.outputs.artifact-prefix }}
      charmcraft-snap-channel: 3.x/edge
    secrets:
      charmhub-token: ${{ secrets.CHARMHUB_TOKEN }}
    permissions:
      contents: write  # Needed to create GitHub release
